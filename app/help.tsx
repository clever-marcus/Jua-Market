import { View, Text } from "react-native";
import { t } from "react-native-tailwindcss";

export default function Help() {
  return (
    <View style={[t.flex1, t.justifyCenter, t.itemsCenter]}>
      <Text>Help Page (coming soon)</Text>
    </View>
  );
}
